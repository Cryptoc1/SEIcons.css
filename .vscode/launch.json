{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Tool",
      "program": "${workspaceFolder}/bin/index.js",
      "preLaunchTask": "${defaultBuildTask}",
      "outFiles": [
        "${workspaceFolder}/bin/**/*.js"
      ]
    }
  ]
}